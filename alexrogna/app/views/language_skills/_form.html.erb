<%= form_with(model: language_skill) do |form| %>
  <% if language_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(language_skill.errors.count, "error") %> prohibited this language_skill from being saved:</h2>

      <ul>
        <% language_skill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_field :language, class: "h-full-width" %>
  </div>

  <div class="field">
    <%= form.label :certificate_name %>
    <%= form.text_field :certificate_name, class: "h-full-width" %>
  </div>

  <div class="field">
    <%= form.label :certificate_url %>
    <%= form.text_field :certificate_url, class: "h-full-width" %>
  </div>

  <div class="field">
    <%= form.label :is_native %>
    <%= form.check_box :is_native %>
  </div>

  <div class="field">
    <%= form.label :procent %>
    <%= form.number_field :procent, class: "h-full-width" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn--submit h-full-width" %>
  </div>
<% end %>
