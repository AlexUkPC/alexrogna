<header class="row">
    <div class="column large-12">
        <h3 class="section-header-allcaps">Personal Skills</h3>
    </div>
</header>

<main class="row personal_skills block-large-1-3 block-medium-1-2 block-tab-1-2">
  <% @personal_skills.each do |personal_skill| %>
    <div class="column item align-center h-text-center">
      <div class="h-add-bottom item" title="<%= personal_skill.description %>">
          <i class='<%= personal_skill.icon %> fa-3x'></i>
          <br>
          <%= personal_skill.skill %>
      </div>
      <% if current_user %>
        <div class="controls">
            <%= link_to edit_personal_skill_path(personal_skill), title: "Edit" do %>
                <i class="fal fa-pen" aria-hidden="true"></i>
            <% end %>
            <%= link_to personal_skill, method: :delete, data: { confirm: 'Are you sure?' }, title: "Delete" do %>
                <i class="fal fa-trash-alt red"></i>
            <% end %>
          </div>
      <% end %>
    </div>
  <% end %>
</main>
<div class="row">
  <div class="align-center">
    <% if current_user %>
      <%= link_to new_personal_skill_path, title: "Add new" do %><i class="fal fa-plus-circle fa-lg" ></i><% end %>
    <% end %>
  </div>
</div>

<footer class="row"></footer>
